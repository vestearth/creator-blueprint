# YouTube templates (deferred)

These templates are preserved for the future YouTube adapter. New v0 content
starts from `templates/core/` and uses `templates/lemon8/` after verification.
Do not use these video-specific files as the canonical content model.
